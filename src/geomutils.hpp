

#ifndef GEOMUTILS_HPP
#define GEOMUTILS_HPP

/**
 * The smoothstep function is used to interpolate between two values.
 */
float smoothstep(float value, float xBegin, float xEnd, float yBegin, float yEnd);

#endif